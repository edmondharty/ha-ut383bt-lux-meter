# Copy this file to deploy.env (gitignored) and fill in your own values.
# deploy.env is sourced by scripts/deploy.sh.

# SSH target — user@hostname or IP
HA_HOST="user@homeassistant.local"

# Absolute path to the custom_components directory on the HA host
HA_TARGET="/config/custom_components/ut353bt"
